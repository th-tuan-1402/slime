NUXT_PUBLIC_API_BASE=http://localhost:8080

# Optional: live OpenAPI document URL when generating the client (e.g. Scramble).
# If unset, `npm run api:gen` uses `openapi/openapi.snapshot.json`.
# OPENAPI_URL=http://localhost:8080/docs/api.json
